import React from 'react';
import './PricingPage.css';

function PricingPage() {
    return (
      // THIS IS WHERE YOU DO THE HTML
      // pro tip: do a div on the outside of everything, or there'll
      // be weird issues
      // just copy this template for every page
      <div className="PricingPage">

      </div>
    );
  }

  export default PricingPage;